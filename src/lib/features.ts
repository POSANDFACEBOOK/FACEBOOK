// สวิตช์เปิด/ปิดฟีเจอร์ของทั้งระบบ (ใช้ได้ทั้งฝั่ง server และหน้าเว็บ)

/** ช่องทาง LINE OA — ตอนนี้ "ซ่อนไว้ก่อน" เพื่อเน้นตอบแชท Facebook อย่างเดียว
 *
 * ปิดอยู่ (ค่าเริ่มต้น):
 * - เพจ LINE ไม่โผล่ในกล่องข้อความ / ตัวเลขแจ้งเตือน / ตั้งค่า / จัดการทีม / ช่องทางแชท
 * - API ที่อ่าน/ส่งแชทมองไม่เห็นเพจ LINE (กรองที่ getCurrentUserContext จุดเดียว)
 * - เชื่อม/ยกเลิก LINE OA ไม่ได้
 * - webhook LINE ยังรับข้อความเก็บไว้ตามปกติ → เปิดกลับมาแล้วประวัติแชทครบ
 * - ข้อมูล LINE ในฐานข้อมูล (OA ที่เชื่อมไว้, แชท, สิทธิ์ลูกทีม) ไม่ถูกลบหรือแก้
 *
 * เปิดกลับ: ตั้ง Environment Variable `NEXT_PUBLIC_ENABLE_LINE=true` ใน Vercel แล้ว Redeploy */
export const LINE_ENABLED = process.env.NEXT_PUBLIC_ENABLE_LINE === 'true'
